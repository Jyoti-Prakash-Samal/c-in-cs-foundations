#include <stdio.h>
int main(vvoid){
    int n;
    printf("Enter a number\n");
    scanf("%d", &n);
    if(&n ==1)
    {
        printf("The number is odd\n");
    }
    else{
        printf("the nummber is even");
    }
    return 0;
}